import styles from './Stats.module.css';
function Stats() {
    return (
        <section className={styles['stats-wrapper']}>
            <h1>stats</h1>
        </section>
    );
}

export default Stats;